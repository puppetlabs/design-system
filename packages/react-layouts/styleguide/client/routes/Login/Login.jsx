import React from 'react';

import Login from 'auth/Login';

import './Login.scss';

/**
 * TODO: it would be great to display an example here of how to use these components.
 * After we implement them in nebula, we should backport that code as an example here
 */
const LoginStyleguide = () => <Login product="insights" />;

export default LoginStyleguide;
