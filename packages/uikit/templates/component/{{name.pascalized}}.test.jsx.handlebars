import React from 'react';
import { shallow } from 'enzyme';

import {{name.pascalized}} from './{{name.pascalized}}';

test('renders without crashing', () => {
  shallow(<{{name.pascalized}} />);
});
