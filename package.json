{
  "name": "root",
  "private": true,
  "scripts": {
    "format": "lerna run format",
    "lint": "lerna run lint",
    "postinstall": "lerna bootstrap --hoist",
    "test": "./scripts/test",
    "start": "cd packages/design-system-website && npm i && npx styleguidist server",
    "styleguidist:build": "cd packages/design-system-website && npm i && npx styleguidist build"
  },
  "devDependencies": {
    "lerna": "^3.13.4"
  }
}
