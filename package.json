{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "format": "lerna run format",
    "lint": "lerna run lint",
    "postinstall": "npm run bootstrap",
    "publish": "./scripts/requireVersionBumps && lerna publish from-package",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "publishConfig": {
    "registry": "https://artifactory.delivery.puppetlabs.net/artifactory/api/npm/npm__local/"
  }
}
