{
  "name": "root",
  "private": true,
  "scripts": {
    "format": "lerna run format",
    "lint": "lerna run lint",
    "postinstall": "lerna bootstrap --hoist",
    "test": "./scripts/test",
    "start": "cd packages/design-system-website && styleguidist server"
  },
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "publishConfig": {
    "registry": "https://artifactory.delivery.puppetlabs.net/artifactory/api/npm/npm__local/"
  }
}
