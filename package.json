{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "publish": "lerna bootstrap --hoist && lerna publish",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "publishConfig": {
    "registry": "https://artifactory.delivery.puppetlabs.net/artifactory/api/npm/npm__local/"
  }
}
